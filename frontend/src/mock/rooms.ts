import { RoomsResponse } from '../types';

const roomsMock: RoomsResponse[] = [
    {
        roomId: 1,
        roomName: 'GameingTeam',
    },
    {
        roomId: 2,
        roomName: 'RPG',
    },
];

export default roomsMock;
